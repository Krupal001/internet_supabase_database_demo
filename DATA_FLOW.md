# 🔄 Data Flow Visualization

## Complete Flow: User Action → UI Update

### Example: User Clicks Increment Button

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER ACTION                             │
│                  User clicks increment button                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                           │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ counter_page.dart                                       │    │
│  │ FloatingActionButton(                                   │    │
│  │   onPressed: () {                                       │    │
│  │     context.read<CounterCubit>().incrementCounter();    │    │
│  │   }                                                      │    │
│  │ )                                                        │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                           │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ counter_cubit.dart                                      │    │
│  │                                                          │    │
│  │ Future<void> incrementCounter() async {                 │    │
│  │   emit(const CounterLoading());  // Show loading       │    │
│  │   final result = await incrementCounterUseCase();       │    │
│  │   result.fold(...);                                     │    │
│  │ }                                                        │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      DOMAIN LAYER                               │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ increment_counter_usecase.dart                          │    │
│  │                                                          │    │
│  │ Future<Result<CounterEntity>> call() async {            │    │
│  │   return await repository.incrementCounter();           │    │
│  │ }                                                        │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                       DATA LAYER                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ counter_repository_impl.dart                            │    │
│  │                                                          │    │
│  │ Future<Result<CounterEntity>> incrementCounter() {      │    │
│  │   try {                                                  │    │
│  │     // Get current counter                              │    │
│  │     final current = await localDataSource.getCounter(); │    │
│  │     // Create new counter with value + 1                │    │
│  │     final newCounter = CounterModel(                    │    │
│  │       value: current.value + 1,                         │    │
│  │       lastUpdated: DateTime.now(),                      │    │
│  │     );                                                   │    │
│  │     // Save new counter                                 │    │
│  │     await localDataSource.saveCounter(newCounter);      │    │
│  │     return Result.success(newCounter);                  │    │
│  │   } catch (e) {                                          │    │
│  │     return Result.failure(CacheFailure(e.toString()));  │    │
│  │   }                                                      │    │
│  │ }                                                        │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                       DATA LAYER                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ counter_local_datasource.dart                           │    │
│  │                                                          │    │
│  │ Future<CounterModel> getCounter() {                     │    │
│  │   return _cachedCounter ?? CounterModel(value: 0);      │    │
│  │ }                                                        │    │
│  │                                                          │    │
│  │ Future<CounterModel> saveCounter(CounterModel counter) {│    │
│  │   _cachedCounter = counter;                             │    │
│  │   return counter;                                        │    │
│  │ }                                                        │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ← ← ← RESULT FLOWS BACK ← ← ←
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                           │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ counter_cubit.dart                                      │    │
│  │                                                          │    │
│  │ result.fold(                                            │    │
│  │   onFailure: (failure) =>                               │    │
│  │     emit(CounterError(failure.message)),                │    │
│  │   onSuccess: (counter) =>                               │    │
│  │     emit(CounterLoaded(counter)),  // New state!       │    │
│  │ );                                                       │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                           │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ counter_page.dart                                       │    │
│  │                                                          │    │
│  │ BlocBuilder<CounterCubit, CounterState>(                │    │
│  │   builder: (context, state) {                           │    │
│  │     if (state is CounterLoaded) {                       │    │
│  │       return Text('${state.counter.value}');            │    │
│  │     }                                                    │    │
│  │     // UI rebuilds with new value!                      │    │
│  │   }                                                      │    │
│  │ )                                                        │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         UI UPDATES                              │
│                Counter value shown on screen: 1 → 2             │
└─────────────────────────────────────────────────────────────────┘
```

## State Transitions

```
Initial State
     ↓
[User clicks button]
     ↓
CounterLoading (shows loading indicator)
     ↓
[Use case executes]
     ↓
[Repository gets and saves data]
     ↓
[Result returns]
     ↓
CounterLoaded (shows counter value)
  OR
CounterError (shows error message)
```

## Layer Communication Rules

```
┌──────────────────────┐
│  PRESENTATION        │  ← Can use Domain
│  (UI, Cubit, States) │  ✅ Uses use cases
└──────────────────────┘  ❌ Cannot use Data directly
          ↓ uses
┌──────────────────────┐
│  DOMAIN              │  ← Pure business logic
│  (Entities, Use      │  ✅ No dependencies
│   Cases, Repos)      │  ❌ No Flutter imports
└──────────────────────┘  ❌ No external libraries
          ↑ implements
┌──────────────────────┐
│  DATA                │  ← Can use Domain
│  (Models, Data       │  ✅ Implements repositories
│   Sources, Repo      │  ✅ Handles data operations
│   Implementations)   │  ❌ Cannot use Presentation
└──────────────────────┘
```

## Dependency Flow

```
main.dart
   ↓ initializes
injection_container.dart
   ↓ registers
┌─────────────────────────────────┐
│ Cubit (Factory)                 │
│   ↓ depends on                  │
│ Use Cases (Singleton)           │
│   ↓ depends on                  │
│ Repository Interface (Domain)   │
│   ↑ implemented by              │
│ Repository Impl (Singleton)     │
│   ↓ depends on                  │
│ Data Source (Singleton)         │
└─────────────────────────────────┘
```

## Result Handling Flow

```
┌─────────────────────────────────────────┐
│         Try to get/save data            │
└─────────────────────────────────────────┘
                  ↓
         ┌────────┴────────┐
         ↓                 ↓
    ✅ Success         ❌ Exception
         ↓                 ↓
  Result.success()   Result.failure()
         ↓                 ↓
    Return data      Return Failure
         ↓                 ↓
         └────────┬────────┘
                  ↓
         result.fold(...)
                  ↓
         ┌────────┴────────┐
         ↓                 ↓
    onSuccess()       onFailure()
         ↓                 ↓
  emit(Loaded)      emit(Error)
         ↓                 ↓
    Show data        Show error
```

## BlocBuilder Rebuild Flow

```
1. Cubit emits new state
   emit(CounterLoaded(counter))
        ↓
2. BlocBuilder detects state change
        ↓
3. builder function called with new state
        ↓
4. Widget tree rebuilds
        ↓
5. UI shows updated value
```

## Complete Example: Load Counter on App Start

```
App starts
   ↓
main.dart initializes dependencies
   ↓
BlocProvider creates CounterCubit
   ↓
CounterPage builds
   ↓
BlocBuilder sees CounterInitial state
   ↓
Calls context.read<CounterCubit>().loadCounter()
   ↓
Cubit emits CounterLoading
   ↓
BlocBuilder rebuilds → shows CircularProgressIndicator
   ↓
Cubit calls GetCounterUseCase
   ↓
Use case calls repository.getCounter()
   ↓
Repository calls dataSource.getCounter()
   ↓
Data source returns CounterModel(value: 0)
   ↓
Repository returns Result.success(counter)
   ↓
Use case returns result to Cubit
   ↓
Cubit folds result
   ↓
Cubit emits CounterLoaded(counter)
   ↓
BlocBuilder rebuilds → shows counter value
   ↓
User sees "0" on screen
```

## Error Handling Flow

```
User action
   ↓
Cubit calls use case
   ↓
Repository tries operation
   ↓
Exception occurs!
   ↓
catch (e) { Result.failure(CacheFailure(e.toString())) }
   ↓
Returns failure to use case
   ↓
Use case returns failure to cubit
   ↓
Cubit folds result
   ↓
onFailure called
   ↓
emit(CounterError(failure.message))
   ↓
BlocBuilder rebuilds
   ↓
Shows error UI with message
   ↓
User sees error and can retry
```

## Key Takeaways

1. **Unidirectional Flow**: Data flows from UI → Domain → Data → back to UI
2. **State Changes**: Cubit emits states, BlocBuilder rebuilds UI
3. **Error Handling**: Exceptions caught and converted to Failures
4. **Result Pattern**: Success or Failure, handled with fold()
5. **Dependency Injection**: All dependencies resolved by GetIt
6. **Separation**: Each layer has clear responsibility

## 🎯 Remember

- **UI triggers actions** → Cubit
- **Cubit calls use cases** → Domain
- **Use cases call repositories** → Domain contracts
- **Repositories implement logic** → Data layer
- **Data sources handle storage** → Data layer
- **Results flow back** → Through all layers
- **States update UI** → BlocBuilder rebuilds
